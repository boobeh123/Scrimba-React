// Note from me: Since concepts are being covered/taught, I will only provide files for my local environment. Code will work if cloned & launched.

// Q: What is wrong with this code?
function addTwoNumbersTogether() {
    return 1 + 2
}
// A: The function adds two numbers together, but will always return a sum of 3.

// The function below adds two numbers and will return a new sum.
function addTwoNumbersTogether2(a, b) {
    return a + b
}
